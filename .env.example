# Base origin for the generator worker API (no trailing slash)
# Example: https://<your-generator>.workers.dev
VITE_API_BASE_URL=

# Optional: Dev proxy target for local development (e.g., http://localhost:8788)
# Only used when running `npm run dev`; not used in production builds
VITE_DEV_API_PROXY_TARGET=
